import pandas as pd


def test_input_data_size():
    dataset = pd.read_csv("../data/diabetes.txt", sep='\t').to_numpy()
    X, y = dataset[:, :-1], dataset[:, -1]
    for data_element in dataset:
        data_element_list = data_element.tolist()
        print(type(data_element_list[1]))
        assert not all(data_element_list), "Got None element in dataset: %s" % data_element_list


test_input_data_size()
